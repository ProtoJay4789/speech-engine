# ElevenHacks #10 — Speech Engine
fastapi>=0.115.0
uvicorn>=0.34.0
python-dotenv>=1.0.0

# ElevenLabs SDK (optional — WebSocket mode works without it)
# elevenlabs>=1.50.0

# LLM (optional — demo mode works without it)
# httpx>=0.27.0
